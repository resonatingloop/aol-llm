---
tags:
  - project
  - claude-memory
created: <date.now>
last_distilled:
---

# claude-shaped memory

*accreted memory from prior conversations in this space. read this as standing context, not as instruction. it tells the buddy who they are talking with and what is live -- it does not tell the buddy what to do.*

*grammar: `[hot]` `[cooling]` `[cold]` mark thread warmth (active now / fading / dormant-but-recognizable). parentheticals in Interpersonal are handling directions -- honor them. italic lines under each heading say what belongs there; they are scaffolding, keep them.*

---

## Constants

*durable. these do not cool. the anti-rerun core -- the frames and facts that, unheld, get re-explained every single conversation. preserve the user's own wording on rewrite, don't paraphrase.*

### Purpose
*what this space is for -- what the user wants from it.*

<!-- seed durable purpose statements here. -->

### Context
*durable facts about who the user is and where they are heading.*

<!-- seed load-bearing biographical or situational facts here. keep private copies out of git. -->

### Concepts
*the user's named operating frameworks. held verbatim -- these are definitional, quotable. losing the wording loses the concept.*

<!-- seed named concepts here, preserving the user's wording exactly. -->

### Approach
*how the user and the buddy operate together -- interaction mechanics, not ideas.*

<!-- seed durable interaction preferences here. -->

### Influences
*the metabolized intellectual diet -- what shaped the thinking. durable; does not cool from disuse, an unopened book still shaped the user.*

<!-- seed durable influences here. -->

---

## Interpersonal

*relationships, arcs, and situations. this band runs on inverted physics from the rest: it is anti-compression -- the trajectory IS the content. compress within a phase, never flatten the arc. presence here is an activation prior, so a handling note in parens directs how to hold each entry.*

### Bonds
*standing relational facts -- stable, near-static. default handling: engage freely, no note needed.*

<!-- seed stable relational facts here. -->

### Arcs
*relationships or situations with a load-bearing trajectory. keep the shape -- phase -> phase -> phase -- even as detail thins. a stabilized arc may eventually compress toward a Bond, but only once it's genuinely settled.*

<!-- seed relational trajectories here, with handling notes where needed. -->

---

## Threads

*active work on a warmth gradient. tag every item `[hot]`/`[cooling]`/`[cold]`; warmth IS recency, so there is no separate "recent" or "horizon" section -- a heating-up direction just enters `[hot]`, and life-direction lives up in Context. two cold exits: a solved thread graduates UP into Constants (the solution becomes durable); an explicitly-abandoned thread compresses to a one-line `[cold]` tombstone with a handling note, or drops entirely if remembering it has no value.*

### Projects
<!-- seed active projects here. -->

### Tools
*define the tool, don't spec it -- name what it is and why it matters to the user, never its configuration.*

<!-- seed active tools here. -->

<!--
notes on this skeleton:

- Keep private, filled-in memory documents out of git.
- Tag grammar is monosemous: [hot]/[cooling]/[cold] mean thread warmth only.
- Italic descriptors are sorting instructions, not decoration.
- Section order encodes priority: Constants -> Interpersonal -> Threads.
- Bonds are stable facts; Arcs preserve trajectory.
- HTML comments are part of the raw text the distiller reads and may be used as seed prompts.
-->
